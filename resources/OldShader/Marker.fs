#version 410
//in vec4 v_color;
//in float linewidth;

out vec4 Color;
void main ()
{
    //vec2 coord = gl_PointCoord-vec2(0.5);
    //if (coord.x > linewidth && coord.y > linewidth)
       // discard;
    Color = vec4(1.0);
}